# Web Framework part
fastapi==0.136.3
uvicorn[standard]==0.48.0
python-multipart>=0.0.12        # multipart form support for FastAPI

# Env module
python-dotenv==1.2.2

# Video Extraction Libraries used
yt-dlp==2026.3.17
youtube-transcript-api==1.2.4   # 1.x: instance-based API, FetchedTranscript.snippets

# ── LangChain + LangGraph 
langchain==1.3.2
langchain-core==1.4.0
langchain-text-splitters==1.1.2 # 
# langchain-google-genai==4.2.4   (Gemini integration in langchain not using in deployment due to vercel build constraint)
langchain-community==0.4.2     
langgraph==1.2.2            
langchain-groq==1.1.2           # Groq

# Google Gemini 
# google-genai==2.7.0             (underlying SDK used by langchain-google-genai) [not using due to vercel build constraint]

# Pinecone Vector DB
pinecone==9.0.1                 # 9.x: list_indexes().names(), status.ready, keyword args

# Cohere Embeddings
cohere==7.0.2                   # embed-english-v3.0, input_type separation

# HTTP + Validation
httpx==0.28.1
pydantic==2.13.4

# ── Groq (fast inference) ─────────────────────────────────────────────────────
groq==0.37.1                    # Groq SDK — llama-3.3-70b-versatile, mixtral-8x7b
